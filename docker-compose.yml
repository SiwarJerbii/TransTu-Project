version: '3.8'

services:
  transtu-api:
    build: .
    container_name: transtu-api
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
      - FLASK_DEBUG=True
    volumes:
      - .:/app
      - /app/venv  # Exclude venv from volume
    restart: unless-stopped